# Notebook

`01_banking77_model_analysis.ipynb` is a concise guided walkthrough. The production-quality, reproducible implementation is in `run_pipeline.py` and `src/banking_intent/`.

Run the main pipeline first so that the notebook can display the committed report outputs.

